package Main;

import java.awt.*;

public class Navigator {
    // something to store the graph (for doing caculations and stuff)
    // something to store the wavepoints (for doing drawing)

    public void draw(Graphics2D pen, double screenWidth, double screenHeight) {
        pen.drawLine(0, 0, 100, 100);
        // draw each waypoint
    }
}
